-- =============================================================================
-- lua/plugins/vim-repeat.lua
-- vim-repeat — makes . repeat plugin operations (nvim-surround, etc.)
--
-- To install (run once in your shell):
--   git clone --depth 1 https://github.com/tpope/vim-repeat \
--     ~/.config/nvim/pack/local/opt/vim-repeat
--
-- No setup required — loading the plugin is all that is needed.
-- =============================================================================

vim.cmd.packadd("vim-repeat")
